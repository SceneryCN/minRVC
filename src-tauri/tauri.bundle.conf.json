{
  "$schema": "https://schema.tauri.app/config/2.0.0",
  "_comment": "仅用于打包：通过 `tauri build --config src-tauri/tauri.bundle.conf.json` 与主配置合并。dev 模式不应使用此文件，否则会因为找不到 sidecar 二进制编译失败。",
  "bundle": {
    "externalBin": ["binaries/rvc-sidecar"]
  }
}
