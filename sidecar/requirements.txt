# =============================================================================
# RVC sidecar dependencies
#
# Python: 3.10 or 3.11 only (fairseq does not build on 3.12+)
# pip: must be <24.1 (fairseq pins omegaconf<2.1; pip>=24.1 rejects its metadata)
#
# GPU (CUDA 12.1):
#   pip install "pip<24.1"
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
# CPU-only (debug):
#   pip install "pip<24.1"
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# =============================================================================

# ---------- Web server ----------
fastapi==0.115.5
uvicorn[standard]==0.32.1
websockets==13.1
pydantic==2.9.2

# ---------- Inference core ----------
# PyTorch stopped publishing macOS Intel (x86_64) wheels after 2.2.x.
torch==2.4.1; platform_machine != "x86_64" or sys_platform != "darwin"
torch==2.2.2; platform_machine == "x86_64" and sys_platform == "darwin"
torchaudio==2.4.1; platform_machine != "x86_64" or sys_platform != "darwin"
torchaudio==2.2.2; platform_machine == "x86_64" and sys_platform == "darwin"
numpy==1.26.4
scipy==1.13.1

# ---------- Audio processing ----------
librosa==0.10.2
soundfile==0.12.1
resampy==0.4.3
numba==0.60.0

# ---------- F0 extraction ----------
torchcrepe==0.0.23
torchfcpe==0.0.4

# ---------- HuBERT / ContentVec ----------
# fairseq only supports Python 3.10/3.11; use --no-build-isolation if build fails
fairseq==0.12.2
einops==0.8.0

# ---------- Retrieval index ----------
faiss-cpu==1.9.0

# ---------- Offline vocal separation (Demucs v4) ----------
demucs==4.0.1

# ---------- Optional pro separation backends ----------
# RoFormer / MDX23C via audio-separator (install separately if needed):
#   pip install audio-separator

# ---------- Utilities ----------
loguru==0.7.2
typer==0.13.0
tqdm==4.67.0
PyYAML==6.0.2
requests==2.32.3

# ---------- Packaging ----------
pyinstaller==6.11.1
